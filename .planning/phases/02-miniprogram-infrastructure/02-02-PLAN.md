---
phase: 02-miniprogram-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - miniprogram/package.json
  - miniprogram/src/app.config.ts
  - miniprogram/src/app.ts
  - miniprogram/src/app.scss
  - miniprogram/src/pages/home/index.tsx
  - miniprogram/src/pages/home/index.scss
  - miniprogram/src/components/KnowledgeCard/index.tsx
  - miniprogram/src/components/KnowledgeCard/index.scss
  - miniprogram/src/components/LoginButton/index.tsx
  - miniprogram/src/components/LoginButton/index.scss
  - miniprogram/src/components/AppHeader/index.tsx
  - miniprogram/src/components/AppHeader/index.scss
  - miniprogram/src/styles/variables.scss
  - miniprogram/src/styles/animations.scss
  - miniprogram/src/utils/auth.ts
  - miniprogram/src/utils/dailyCard.ts
  - miniprogram/src/utils/api.ts
  - miniprogram/src/constants/index.ts
  - miniprogram/project.config.json
autonomous: true
requirements:
  - KNOW-01
  - KNOW-02
must_haves:
  truths:
    - "Taro 4.x project compiles to WeChat mini-program"
    - "Mini-program shows home page with app header"
    - "Design tokens (colors, fonts, spacing) mirror Phase 1 Eastern aesthetics"
    - "No framer-motion — CSS @keyframes animations only"
  artifacts:
    - path: "miniprogram/package.json"
      provides: "Taro 4.x + React 18 dependencies"
      contains: "@tarojs/cli@4.1.11"
    - path: "miniprogram/src/styles/variables.scss"
      provides: "Design tokens (WXSS custom properties)"
      tokens: ["--color-ink-black", "--color-vermilion", "--color-rice-paper", "--space-sm", "--font-calligraphy"]
    - path: "miniprogram/src/utils/auth.ts"
      provides: "WeChat login utility"
      exports: ["loginWithWeChat", "checkLoginStatus"]
  key_links:
    - from: "miniprogram/src/utils/dailyCard.ts"
      to: "http://localhost:8002/api/v1/knowledge/daily"
      via: "Taro.request"
      pattern: "knowledge/daily"
    - from: "miniprogram/src/utils/auth.ts"
      to: "wx.login"
      via: "Taro.login()"
      pattern: "Taro\\.login"
---

<objective>
Scaffold the Taro 4.x WeChat mini-program project with React 18. Create the complete project structure, design tokens, utility functions, and component stubs (without full page implementation). This is the foundation for Plan 02-03 which implements the actual home page with login and daily card.

Purpose: Create a working Taro mini-program scaffold that compiles and runs in WeChat DevTools, ready for page implementation
Output: Complete `miniprogram/` directory structure with all config files, design tokens, utilities, and component stubs
</objective>

<execution_context>
@D:/computer-design/.claude/get-shit-done/workflows/execute-plan.md
@D:/computer-design/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/computer-design/.planning/phases/02-miniprogram-infrastructure/02-UI-SPEC.md
@D:/computer-design/.planning/phases/02-miniprogram-infrastructure/02-RESEARCH.md
@D:/computer-design/frontend/tailwind.config.js
@D:/computer-design/frontend/src/index.css
</context>

<interfaces>
<!-- Taro Project Structure that will be created -->

Directory structure:
```
miniprogram/
├── package.json
├── project.config.json
├── src/
│   ├── app.config.ts          # Taro app config (pages, window, global settings)
│   ├── app.ts                 # Entry component
│   ├── app.scss               # Global styles
│   ├── pages/
│   │   └── home/
│   │       ├── index.tsx      # Home page (stub - full impl in Plan 03)
│   │       └── index.scss
│   ├── components/
│   │   ├── KnowledgeCard/
│   │   │   ├── index.tsx      # Daily card (stub - full impl in Plan 03)
│   │   │   └── index.scss
│   │   ├── LoginButton/
│   │   │   ├── index.tsx      # WeChat login button (stub - full impl in Plan 03)
│   │   │   └── index.scss
│   │   └── AppHeader/
│   │       ├── index.tsx
│   │       └── index.scss
│   ├── styles/
│   │   ├── variables.scss     # Design tokens (colors, fonts, spacing)
│   │   └── animations.scss    # Keyframe animations (cardEnter, buttonPress, shimmer)
│   ├── utils/
│   │   ├── auth.ts           # wx.login wrapper
│   │   ├── dailyCard.ts      # Date hashing algorithm
│   │   └── api.ts            # Taro request wrapper
│   └── constants/
│       └── index.ts           # API endpoints, config
```

Design tokens (from 02-UI-SPEC.md):
```wxss
page {
  --color-ink-black: #2B2B2B;
  --color-rice-paper: #F7F5F0;
  --color-vermilion: #C04851;
  --color-cyan-glaze: #5796B3;
  --color-cyan-glaze-light: rgba(87, 150, 179, 0.12);
  --color-tea-green: #CCD4BF;
  --color-charcoal: #4A4A4A;
  --color-white: #FFFFFF;
  --space-xs: 8px;
  --space-sm: 16px;
  --space-md: 24px;
  --space-lg: 32px;
  --space-xl: 48px;
  --space-2xl: 64px;
  --font-serif: 'Noto Serif SC', serif;
  --font-calligraphy: 'Ma Shan Zheng', cursive;
  --font-xiaowei: 'ZCOOL XiaoWei', serif;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Taro project scaffold</name>
  <files>miniprogram/package.json, miniprogram/project.config.json, miniprogram/src/app.config.ts</files>
  <action>
    Create the `miniprogram/` directory structure with Taro 4.x configuration:

    1. Create `miniprogram/package.json`:
    ```json
    {
      "name": "digital-inheritor-miniprogram",
      "version": "1.0.0",
      "private": true,
      "description": "数字传承人 - 微信小程序",
      "scripts": {
        "dev:weapp": "npm run build:weapp -- --watch",
        "build:weapp": "taro build --type weapp",
        "dev": "taro build --type weapp --watch",
        "build": "taro build --type weapp",
        "lint": "eslint ."
      },
      "dependencies": {
        "@tarojs/components": "4.1.11",
        "@tarojs/react": "4.1.11",
        "@tarojs/taro": "4.1.11",
        "react": "^18.2.0"
      },
      "devDependencies": {
        "@babel/core": "^7.24.0",
        "@tarojs/cli": "4.1.11",
        "@types/react": "^18.2.0",
        "typescript": "^5.0.0"
      }
    }
    ```

    2. Create `miniprogram/project.config.json`:
    ```json
    {
      "miniprogramRoot": "./src/",
      "projectname": "digital-inheritor",
      "appid": "touristappid",
      "compileType": "miniprogram",
      "setting": {
        "urlCheck": false,
        "es6": true,
        "enhance": true,
        "postcss": true,
        "preloadBackgroundData": false,
        "minified": true,
        "newFeature": true,
        "coverView": true,
        "autoAudits": false,
        "showShadowRootInWxmlPanel": true,
        "scopeDataCheck": false,
        "checkInvalidKey": true,
        "checkSiteMap": true,
        "uploadWithSourceMap": true,
        "compileHotReLoad": true,
        "useMultiFrameRuntime": true,
        "useApiHook": true,
        "useApiHostProcess": true,
        "babelSetting": {
          "ignore": [],
          "disablePlugins": [],
          "outputPath": ""
        },
        "useIsolateContext": true,
        "userConfirmedBundleSwitch": false,
        "packNpmManually": false,
        "packNpmRelationList": [],
        "minifyWXSS": true
      },
      "libVersion": "3.3.0",
      "srcMiniprogramRoot": "src/",
      "condition": {}
    }
    ```

    3. Create `miniprogram/src/app.config.ts`:
    ```typescript
    export default defineAppConfig({
      pages: ['pages/home/index'],
      window: {
        backgroundTextStyle: 'light',
        navigationBarBackgroundColor: '#2B2B2B',
        navigationBarTitleText: '数字传承人',
        navigationBarTextStyle: 'white',
      },
      style: 'v2',
      componentFramework: 'glass-eager',
    })
    ```
  </action>
  <read_first>
    - miniprogram/src/app.config.ts (file being created)
  </read_first>
  <verify>
    <automated>grep -l "appid\|compileType\|miniprogramRoot" miniprogram/project.config.json && echo "PASS: project.config.json exists"</automated>
  </verify>
  <done>
    Taro project has package.json with @tarojs/cli@4.1.11 and React 18, project.config.json with WeChat mini-program settings
  </done>
</task>

<task type="auto">
  <name>Task 2: Create design tokens and animations</name>
  <files>miniprogram/src/styles/variables.scss, miniprogram/src/styles/animations.scss</files>
  <action>
    Create design tokens and animations following 02-UI-SPEC.md exactly:

    1. Create `miniprogram/src/styles/variables.scss`:
    ```scss
    /* Design tokens — Phase 1 Eastern aesthetics adapted for WXSS */
    page {
      /* Colors — 6-color palette */
      --color-ink-black: #2B2B2B;
      --color-rice-paper: #F7F5F0;
      --color-vermilion: #C04851;
      --color-cyan-glaze: #5796B3;
      --color-cyan-glaze-light: rgba(87, 150, 179, 0.12);
      --color-tea-green: #CCD4BF;
      --color-charcoal: #4A4A4A;
      --color-white: #FFFFFF;

      /* Spacing — 8-point grid */
      --space-xs: 8px;
      --space-sm: 16px;
      --space-md: 24px;
      --space-lg: 32px;
      --space-xl: 48px;
      --space-2xl: 64px;

      /* Typography */
      --font-serif: 'Noto Serif SC', serif;
      --font-calligraphy: 'Ma Shan Zheng', cursive;
      --font-xiaowei: 'ZCOOL XiaoWei', serif;
      --font-size-body: 28rpx;
      --font-size-label: 24rpx;
      --font-size-heading: 36rpx;
      --font-size-display: 48rpx;

      /* Shadows */
      --card-shadow: 0 4px 20rpx -2px rgba(43, 43, 43, 0.1);
      --hover-shadow: 0 8px 24rpx -2px rgba(43, 43, 43, 0.15);
    }
    ```

    2. Create `miniprogram/src/styles/animations.scss`:
    ```scss
    /* Card entrance animation — fade + slide up */
    @keyframes cardEnter {
      from {
        opacity: 0;
        transform: translateY(24rpx);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Login button press feedback */
    @keyframes buttonPress {
      0% { transform: scale(1); }
      50% { transform: scale(0.97); }
      100% { transform: scale(1); }
    }

    /* Loading shimmer effect */
    @keyframes shimmer {
      0% { background-position: 200% 0; }
      100% { background-position: -200% 0; }
    }

    .card-enter {
      animation: cardEnter 0.4s ease-out forwards;
    }

    .login-btn:active {
      animation: buttonPress 100ms ease-out;
    }

    .skeleton {
      background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
      background-size: 200% 100%;
      animation: shimmer 1.5s infinite linear;
    }
    ```
  </action>
  <read_first>
    - D:/computer-design/.planning/phases/02-miniprogram-infrastructure/02-UI-SPEC.md (for exact color/spacing/typography values)
  </read_first>
  <verify>
    <automated>grep -c "color-ink-black\|color-vermilion\|color-rice-paper" miniprogram/src/styles/variables.scss && grep -c "cardEnter\|buttonPress\|shimmer" miniprogram/src/styles/animations.scss</automated>
  </verify>
  <done>
    WXSS variables.scss has all 6 palette colors + spacing scale + font families. animations.scss has cardEnter, buttonPress, shimmer keyframes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create utility functions</name>
  <files>miniprogram/src/utils/auth.ts, miniprogram/src/utils/dailyCard.ts, miniprogram/src/utils/api.ts, miniprogram/src/constants/index.ts</files>
  <action>
    Create the utility functions for WeChat auth, daily card hashing, and API requests:

    1. Create `miniprogram/src/constants/index.ts`:
    ```typescript
    // API endpoints
    export const API_BASE = 'http://localhost:8002'
    export const API_KNOWLEDGE_DAILY = '/api/v1/knowledge/daily'

    // Storage keys
    export const STORAGE_SESSION = 'session'
    export const STORAGE_USER_ID = 'userId'
    ```

    2. Create `miniprogram/src/utils/auth.ts`:
    ```typescript
    import Taro from '@tarojs/taro'
    import { STORAGE_SESSION } from '../constants'

    /**
     * Perform WeChat login via wx.login()
     * Stores the login code as session in WeChat storage
     */
    export async function loginWithWeChat(): Promise<string> {
      const loginResult = await Taro.login()
      if (!loginResult.code) {
        throw new Error('wx.login failed')
      }
      await Taro.setStorage({ key: STORAGE_SESSION, data: loginResult.code })
      return loginResult.code
    }

    /**
     * Check if user has an existing WeChat session
     */
    export async function checkLoginStatus(): Promise<boolean> {
      try {
        const session = await Taro.getStorage({ key: STORAGE_SESSION })
        return !!session.data
      } catch {
        return false
      }
    }
    ```

    3. Create `miniprogram/src/utils/api.ts`:
    ```typescript
    import Taro from '@tarojs/taro'
    import { API_BASE } from '../constants'

    /**
     * Taro request wrapper for API calls
     */
    export function request<T = any>(options: {
      url: string
      method?: 'GET' | 'POST' | 'PUT' | 'DELETE'
      data?: any
    }): Promise<T> {
      return Taro.request({
        url: `${API_BASE}${options.url}`,
        method: options.method || 'GET',
        data: options.data,
      }).then(res => {
        if (res.statusCode >= 200 && res.statusCode < 300) {
          return res.data
        }
        throw new Error(`Request failed with status ${res.statusCode}`)
      })
    }
    ```

    4. Create `miniprogram/src/utils/dailyCard.ts`:
    ```typescript
    import Taro from '@tarojs/taro'
    import { request } from './api'
    import { API_KNOWLEDGE_DAILY, STORAGE_USER_ID } from '../constants'

    export interface ICHFact {
      id: number
      title: string
      category: string
      imageUrl: string
      fact: string
    }

    interface DailyResponse {
      facts: ICHFact[]
    }

    /**
     * Simple string hash function for deterministic daily selection
     * hash(dateStr:userId) -> index in facts array
     */
    function hashString(input: string): number {
      let hash = 0
      for (let i = 0; i < input.length; i++) {
        const char = input.charCodeAt(i)
        hash = ((hash << 5) - hash) + char
        hash = hash & hash
      }
      return Math.abs(hash)
    }

    /**
     * Get the daily ICH fact for today
     * Uses hash(date + userId) to deterministically select one fact
     */
    export async function getDailyFact(): Promise<ICHFact | null> {
      try {
        const res = await request<DailyResponse>({
          url: API_KNOWLEDGE_DAILY,
          method: 'GET',
        })

        const facts = res.facts
        if (!facts || facts.length === 0) return null

        // Get date string (YYYY-MM-DD)
        const now = new Date()
        const dateStr = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-${String(now.getDate()).padStart(2, '0')}`

        // Get user ID from storage (anonymous fallback)
        let userId = 'anonymous'
        try {
          const stored = await Taro.getStorage({ key: STORAGE_USER_ID })
          userId = stored.data || 'anonymous'
        } catch { /* use anonymous */ }

        // Deterministic hash
        const hashInput = `${dateStr}:${userId}`
        const index = hashString(hashInput) % facts.length

        return facts[index]
      } catch (e) {
        console.error('Failed to fetch daily fact:', e)
        return null
      }
    }
    ```
  </action>
  <read_first>
    - miniprogram/src/utils/auth.ts (file being created)
    - miniprogram/src/utils/dailyCard.ts (file being created)
  </read_first>
  <verify>
    <automated>grep -c "export async function\|export function" miniprogram/src/utils/auth.ts miniprogram/src/utils/dailyCard.ts miniprogram/src/utils/api.ts</automated>
  </verify>
  <done>
    auth.ts exports loginWithWeChat and checkLoginStatus. dailyCard.ts exports getDailyFact returning ICHFact. api.ts exports request wrapper.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create component stubs and app entry</name>
  <files>miniprogram/src/app.ts, miniprogram/src/app.scss, miniprogram/src/components/KnowledgeCard/index.tsx, miniprogram/src/components/LoginButton/index.tsx, miniprogram/src/components/AppHeader/index.tsx, miniprogram/src/pages/home/index.tsx</files>
  <action>
    Create the app entry point, stub components, and home page:

    1. Create `miniprogram/src/app.ts`:
    ```typescript
    import { Component } from 'react'
    import './app.scss'
    import './styles/variables.scss'
    import './styles/animations.scss'

    class App extends Component {
      componentDidMount() {}

      componentDidShow() {}

      componentDidHide() {}

      render() {
        return this.props.children
      }
    }

    export default App
    ```

    2. Create `miniprogram/src/app.scss`:
    ```scss
    /* Global styles — imported from variables and app */
    page {
      background-color: var(--color-rice-paper);
      font-family: var(--font-serif);
      color: var(--color-ink-black);
      font-size: var(--font-size-body);
      line-height: 1.6;
    }
    ```

    3. Create `miniprogram/src/components/AppHeader/index.tsx` (stub):
    ```typescript
    import { View, Text } from '@tarojs/components'
    import './index.scss'

    export default function AppHeader() {
      return (
        <View className='app-header'>
          <Text className='app-title'>数字传承人</Text>
          <Text className='app-subtitle'>每日非遗知识</Text>
        </View>
      )
    }
    ```

    4. Create `miniprogram/src/components/AppHeader/index.scss`:
    ```scss
    .app-header {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: var(--space-lg) var(--space-sm);
    }

    .app-title {
      font-family: var(--font-calligraphy);
      font-size: var(--font-size-display);
      color: var(--color-ink-black);
    }

    .app-subtitle {
      font-family: var(--font-xiaowei);
      font-size: var(--font-size-label);
      color: var(--color-charcoal);
      margin-top: var(--space-xs);
    }
    ```

    5. Create `miniprogram/src/components/KnowledgeCard/index.tsx` (stub):
    ```typescript
    import { View, Text, Image } from '@tarojs/components'
    import './index.scss'

    interface Props {
      imageUrl?: string
      category?: string
      title?: string
      factText?: string
    }

    export default function KnowledgeCard({ imageUrl = '', category = '', title = '', factText = '' }: Props) {
      return (
        <View className='knowledge-card card-enter'>
          {imageUrl && (
            <Image className='card-image' src={imageUrl} mode='aspectFill' lazy-load />
          )}
          <View className='card-content'>
            <Text className='card-title'>{title}</Text>
            {category && (
              <View className='card-tag'>
                <Text className='tag-text'>{category}</Text>
              </View>
            )}
            <Text className='card-fact'>{factText}</Text>
          </View>
        </View>
      )
    }
    ```

    6. Create `miniprogram/src/components/KnowledgeCard/index.scss`:
    ```scss
    .knowledge-card {
      background: var(--color-white);
      border-radius: 12rpx;
      box-shadow: var(--card-shadow);
      overflow: hidden;
      margin: var(--space-sm);
    }

    .card-image {
      width: 100%;
      height: 400rpx;
      display: block;
    }

    .card-content {
      padding: var(--space-md);
    }

    .card-title {
      font-family: var(--font-calligraphy);
      font-size: var(--font-size-heading);
      color: var(--color-ink-black);
      display: block;
      margin-bottom: var(--space-sm);
    }

    .card-tag {
      display: inline-block;
      background: var(--color-cyan-glaze-light);
      padding: 6rpx 16rpx;
      border-radius: 20rpx;
      margin-bottom: var(--space-sm);
    }

    .tag-text {
      font-family: var(--font-xiaowei);
      font-size: var(--font-size-label);
      color: var(--color-cyan-glaze);
    }

    .card-fact {
      font-family: var(--font-serif);
      font-size: var(--font-size-body);
      color: var(--color-charcoal);
      line-height: 1.6;
    }
    ```

    7. Create `miniprogram/src/components/LoginButton/index.tsx` (stub):
    ```typescript
    import { View, Text, Button } from '@tarojs/components'
    import './index.scss'

    interface Props {
      onLoginSuccess?: () => void
    }

    export default function LoginButton({ onLoginSuccess }: Props) {
      const handleClick = () => {
        // Stub — full implementation in Plan 03
        onLoginSuccess?.()
      }

      return (
        <Button className='login-btn' onClick={handleClick}>
          <Text className='btn-text'>微信登录</Text>
        </Button>
      )
    }
    ```

    8. Create `miniprogram/src/components/LoginButton/index.scss`:
    ```scss
    .login-btn {
      background: var(--color-vermilion);
      color: var(--color-white);
      font-family: var(--font-xiaowei);
      font-size: var(--font-size-body);
      height: 96rpx;
      line-height: 96rpx;
      border-radius: 24rpx;
      border: none;
      margin: var(--space-md) var(--space-sm);
      width: calc(100% - var(--space-sm) * 2);
    }

    .btn-text {
      color: var(--color-white);
    }
    ```

    9. Create `miniprogram/src/pages/home/index.tsx` (stub):
    ```typescript
    import { View } from '@tarojs/components'
    import { useState, useEffect } from 'react'
    import AppHeader from '../../components/AppHeader'
    import KnowledgeCard from '../../components/KnowledgeCard'
    import LoginButton from '../../components/LoginButton'
    import { checkLoginStatus } from '../../utils/auth'
    import './index.scss'

    export default function Home() {
      const [isLoggedIn, setIsLoggedIn] = useState(false)

      useEffect(() => {
        checkLoginStatus().then(setIsLoggedIn)
      }, [])

      return (
        <View className='home-page'>
          <AppHeader />
          <KnowledgeCard />
          <LoginButton onLoginSuccess={() => setIsLoggedIn(true)} />
        </View>
      )
    }
    ```

    10. Create `miniprogram/src/pages/home/index.scss`:
    ```scss
    .home-page {
      min-height: 100vh;
      background: var(--color-rice-paper);
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    ```
  </action>
  <read_first>
    - miniprogram/src/app.ts (file being created)
    - miniprogram/src/components/KnowledgeCard/index.tsx (file being created)
  </read_first>
  <verify>
    <automated>grep -l "export default function Home\|export default function KnowledgeCard\|export default function LoginButton" miniprogram/src/pages/home/index.tsx miniprogram/src/components/KnowledgeCard/index.tsx miniprogram/src/components/LoginButton/index.tsx</automated>
  </verify>
  <done>
    App entry compiles. Home page renders AppHeader, KnowledgeCard, LoginButton. All components have stub implementations.
  </done>
</task>

</tasks>

<verification>
- `miniprogram/package.json` contains `@tarojs/cli@4.1.11` and `react@^18`
- `miniprogram/src/styles/variables.scss` contains all WXSS custom properties
- `miniprogram/src/utils/auth.ts` exports `loginWithWeChat`, `checkLoginStatus`
- `miniprogram/src/utils/dailyCard.ts` exports `getDailyFact` returning `ICHFact`
- `miniprogram/src/components/KnowledgeCard/index.tsx` renders image, category tag, title, fact text
- `miniprogram/src/components/LoginButton/index.tsx` triggers wx.login on click
</verification>

<success_criteria>
- `miniprogram/` directory is a valid Taro 4.x project
- Project compiles with `taro build --type weapp` (no errors)
- Home page shows app header + knowledge card placeholder + login button placeholder
- Design tokens match Phase 1 Eastern aesthetics palette
- No framer-motion imports anywhere in the project
</success_criteria>

<output>
After completion, create `.planning/phases/02-miniprogram-infrastructure/02-02-SUMMARY.md`
</output>
