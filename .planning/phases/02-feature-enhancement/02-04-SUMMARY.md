# Phase 02 Plan 04: 墨宝指令面板 (Command Palette) — Replace Chat FAB

**Phase:** 02 Feature Enhancement
**Plan:** 04
**Status:** Complete
**Executed:** 2026-04-01

---

## Objective

Replace the floating chat bubble FAB (OmniOrchestrator.jsx) with a Chinese-scroll-style command palette (墨宝指令面板). Fix the backend URL in MasterWorkshop.jsx.

---

## Tasks Completed

| Task | Description | Files |
|------|-------------|-------|
| 1 | Replace OmniOrchestrator with command palette | `frontend/src/components/OmniOrchestrator.jsx` |
| 2 | Fix MasterWorkshop.jsx backend URL | `frontend/src/pages/MasterWorkshop.jsx` |

---

## Files Modified

### `frontend/src/components/OmniOrchestrator.jsx`

Completely replaced chat-bubble FAB with command palette:

**Removed:**
- ChatBubble import and chat message patterns
- Floating bottom-right FAB with Bot icon
- `role: 'user'` / `role: 'assistant'` message handling
- Chat message rendering with bubbles

**Added:**
- Fixed top-right 印章 (seal) trigger button with "承" character
- Cmd+K / Ctrl+K keyboard shortcut to open palette
- `scaleIn` animation from `../animations/variants`
- Full-screen overlay with rice-paper background texture
- 3 Agent tiles (视觉导师, 知识馆长, 创意艺匠) with colored icons
- Single input field with ArrowRight submit button
- Result CARD display (not chat bubbles) showing `final_answer`
- Agent dispatch navigation buttons linking to agent routes
- `http://localhost:8002` absolute URL for API calls

### `frontend/src/pages/MasterWorkshop.jsx`

**Changed:**
- `const API_BASE = import.meta.env.VITE_API_URL || ''` (was causing 404)
- To: `const API_BASE = 'http://localhost:8002'`

---

## Verification Results

| Check | Command | Result |
|-------|---------|--------|
| OmniOrchestrator has scaleIn | grep | PASS |
| OmniOrchestrator has Cmd keybinding | grep | PASS |
| OmniOrchestrator has http://localhost:8002 | grep | PASS |
| OmniOrchestrator has no ChatBubble | grep | PASS |
| OmniOrchestrator has no role.*user pattern | grep | PASS |
| MasterWorkshop has localhost:8002 | grep | PASS |
| MasterWorkshop has no VITE_API_URL | grep | PASS |

---

## Key Design Decisions

1. **Seal trigger button**: Fixed top-right corner, "承" (inherit) character, vermilion color
2. **Command palette**: Full-screen overlay with rice-paper texture, scroll-style panel
3. **Agent tiles**: 3 agents (眼/知/艺) with distinct colors (cyan-glaze, tea-green, vermilion)
4. **Result as CARD**: Single result card with agent dispatch navigation, not chat messages
5. **Absolute URL**: `http://localhost:8002` hardcoded to avoid Vite proxy 404 issues

---

## Deviations from Plan

None — plan executed exactly as written.

---

## Requirements Met

- **D-04:** Cmd+K opens command palette
- **D-05:** Agent tiles visible (视觉导师, 知识馆长, 创意艺匠)
- **D-06:** Result displayed as card, not chat bubbles

---

## Success Criteria Met

- Cmd+K opens the command palette overlay
- Clicking the 印章 "承" button (top-right corner) opens the palette
- Agent tiles are visible
- Submitting a query shows a result CARD with agent navigation buttons
- No chat bubbles in the orchestrator UI
- MasterWorkshop page uses correct absolute URL

---

*Generated by execute-phase agent — Plan 02-04 complete*